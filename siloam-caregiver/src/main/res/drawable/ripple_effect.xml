<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <!-- Optional: Add a background color behind the ripple -->
    <item android:id="@android:id/mask">
        <color android:color="#E0E0E0"/> <!-- Change to desired mask color -->
    </item>

    <!-- Content that the ripple effect should apply to -->
    <item android:drawable="@android:color/white"/> <!-- Change to your background color -->

</ripple>