<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/iv_picture_item"
        android:layout_width="80dp"
        android:layout_height="80dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />


    <ImageView
        android:id="@+id/iv_play_button"
        android:layout_width="@dimen/large_fit"
        android:layout_height="@dimen/large_fit"
        android:src="@drawable/ic_play_video"
        android:translationZ="1dp"
        android:visibility="gone"
        app:layout_constraintStart_toStartOf="@id/iv_picture_item"
        app:layout_constraintTop_toTopOf="@id/iv_picture_item"
        app:layout_constraintEnd_toEndOf="@id/iv_picture_item"
        app:layout_constraintBottom_toBottomOf="@id/iv_picture_item"/>

</androidx.constraintlayout.widget.ConstraintLayout>
